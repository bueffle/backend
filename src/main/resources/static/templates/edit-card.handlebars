      <div class="modal fade" id="editCardModal" tabindex="-1" role="dialog" aria-labelledby="editCardModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="editCardModalLabel">Karte editieren</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="card_question">Frage</label>
                        <input type="text" class="form-control" id="card_question" placeholder="Bitte gib eine Frage ein" value="{{question}}">
                    </div>
                    <div class="form-group">
                        <label for="card_answer">Antwort</label>
                        <input type="text" class="form-control" id="card_answer" placeholder="Bitte gibt die passende Antwort ein" value="{{answer}}">
                    </div>
                    {{#if owner}}
                    <div class="form-group">
                        <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="card_public" {{#if public}}checked{{/if}}>
                        <label class="form-check-label" for="card_public">
                            Karte ist öffentlich
                        </label>
                        </div>
                    </div>
                    {{/if}}
                </form>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Abbrechen</button>
              <button type="button" onclick="saveCard({{id}})" class="btn btn-primary">Speichern</button>
            </div>
          </div>
        </div>
      </div>